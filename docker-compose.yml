services:
  marketing-parser:
    build: .
    env_file:
      - .env.example
    environment:
      DATABASE_URL: sqlite:////app/data/marketing_parser.db
      AUTO_CREATE_SCHEMA: "false"
      AUTH_ENABLED: "true"
      IDENTITY_SECRET_KEY: dev-change-me-32-byte-secret-key-for-turkuaz-identity
      IDENTITY_ALGORITHM: HS256
      DEV_ADMIN_LOGIN_ENABLED: "false"
    ports:
      - "8503:8503"
    volumes:
      - marketing_parser_data:/app/data
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8503/api/v1/ready', timeout=2)",
        ]
      interval: 10s
      timeout: 3s
      retries: 10

  marketing-parser-web:
    build:
      context: ..
      dockerfile: marketing_parser/frontend/Dockerfile
    environment:
      VITE_PROXY_TARGET: http://marketing-parser:8503
      VITE_IDENTITY_PROXY_TARGET: http://host.docker.internal:8500
      VITE_IDENTITY_API_FALLBACK_BASE_URL: http://localhost:8500/api/v1
    ports:
      - "7503:7503"
    depends_on:
      marketing-parser:
        condition: service_healthy

volumes:
  marketing_parser_data:
